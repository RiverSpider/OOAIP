name: TurnTest

on:
  push:
    branches: ["Rotation"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET7
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 7.0.x

      - name: Restore dependencies
        run: dotnet restore SpaceBattle

      - name: Test Build
        run: dotnet build SpaceBattle --no-restore /p:ContinuousIntegrationBuild=true

      - name: Turn Test
        run: dotnet test Tests /p:CollectCoverage=true /p:CoverletOutput=TestResults/ /p:CoverletOutputFormat=lcov